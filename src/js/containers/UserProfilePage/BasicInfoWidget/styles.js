import styled from 'styled-components';

export const BasicInfoWidgetWrapper = styled.div`
    display: flex;
    position: relative;
    width: 100%;
    height: 100%;
    flex-wrap: wrap;
    flex-direction: column;
    justify-content: start;
    align-items: start;
`;

export const BasicInfoFieldsWrapper = styled.div`
    margin: 20px 0;
`;